import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        primary:{
           main:'#64ffda' 
        },
        secondary:{
            main:'#b3e5fc'
        }
    }
})

export default theme